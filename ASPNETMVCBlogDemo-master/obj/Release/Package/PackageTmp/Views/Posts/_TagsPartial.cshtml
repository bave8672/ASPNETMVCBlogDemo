@model IEnumerable<MVCBlogDemo.Models.Tag>

@if (Model != null & Model.Any())
{
    <div class="tagsPartial">
        <div class="tagsTitle">Tags:</div>
        @foreach (var tag in Model)
        {
            <a href="~/Search/?q=@tag.Name" class="tag">@tag.Name</a>
        }
    </div>
}
